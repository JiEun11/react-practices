import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
// ReactDOM.render(App(), document.getElementById('root'));
// (createElement.App(),null,{})
// (createElement(createElement('h1')),null,{}) 이렇게 바뀜